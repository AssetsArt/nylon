tls:
  - type: custom
    cert: ./examples/cert/localhost.crt
    key: ./examples/cert/localhost.key
    # chain: # Optional
    #   - ./examples/cert/chain.pem
    domains:
      - localhost
      - app.localhost
      - api.localhost
      - admin.localhost

  # - type: acme
  #   email: test@example.com
  #   provider: letsencrypt # Options: letsencrypt, buypass
  #   domains:
  #     - example.com
  #     - api.example.com
  #     - admin.example.com