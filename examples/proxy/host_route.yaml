
# https://github.com/ibraheemdev/matchit

# host route
routes:
  - route:
      type: host
      value: localhost # domain.com|domain2.com|domain3.com
    name: http-route-1
    paths:
      - path:
          - /ws
        methods:
          - GET
          - POST
          - OPTIONS
        service:
          name: ws-service
      - path: 
          - /
          - /{*path}
        methods:
          - GET
          - POST
          - OPTIONS
        middleware:
          - group: example
        service:
          name: http-service
