routes:
  - route:
      type: host
      value: localhost
    name: http-route-1
    paths:
      - pathType: Prefix
        methods:
          - GET
        path: /
        service:
          name: http-service