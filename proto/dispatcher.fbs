namespace nylon_dispatcher;

table NylonDispatcher {
  http_end: bool;
  request_id: string (required);
  name: string;
  entry: string;
  data: [ubyte] (required);
  payload: [ubyte];
  store: [ubyte];
}

root_type NylonDispatcher;
